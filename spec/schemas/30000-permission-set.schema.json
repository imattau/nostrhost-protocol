{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/imattau/nostrhost/blob/main/authority/event-protocol/schemas/30000-permission-set.schema.json",
  "title": "NIP-51 kind 30000 — permission set",
  "description": "Addressable. d = permission name. Public entries are p=<pubkey 64-hex> tags; private entries are NIP-44 encrypted in content. An empty content string is allowed.",
  "type": ["object", "null"],
  "properties": {
    "public": { "type": "boolean" }
  },
  "additionalProperties": true
}
